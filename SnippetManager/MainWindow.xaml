<Window x:Class="SnippetManager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:local="clr-namespace:SnippetManager"
        xmlns:viewmodel="clr-namespace:SnippetManager.Viewmodels"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">

    <Window.DataContext>
        <viewmodel:SnippetManagerViewmodel/>
    </Window.DataContext>
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200px"></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <Grid Grid.RowSpan="2" Grid.Column="0" Margin="5">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"></RowDefinition>
                <RowDefinition Height="Auto"></RowDefinition>
            </Grid.RowDefinitions>
            <ListBox Grid.Row="0" ItemsSource="{Binding Snippets}" SelectedItem="{Binding SelectedSnippet}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Title}"></TextBlock>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
            <Button Grid.Row="1" Margin="0,5,0,0" >추가</Button>
        </Grid>

        <DockPanel Grid.Column="1" Margin="0,5,5,0">
            <Button Margin="0,0,0,0">복사</Button>
            <Button Margin="5,0,0,0">저장</Button>
            <TextBox Margin="5,0,0,0" Text="{Binding SelectedSnippet.Title, Mode=OneWay}"></TextBox>
        </DockPanel>
        <TextBox Grid.Row="1" Grid.Column="1" Margin="0,5,5,5" Text="{Binding SelectedSnippet.Content, Mode=OneWay}"/>

    </Grid>
</Window>
