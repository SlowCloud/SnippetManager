<Window x:Class="SnippetManager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SnippetManager"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200px"></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <ListBox Grid.RowSpan="2" Grid.Column="0" Margin="5" d:ItemsSource="{d:SampleData ItemCount=5}"/>

        <DockPanel Grid.Column="1" Margin="0,5,5,0">
            <Button Margin="0,0,0,0">복사</Button>
            <Button Margin="5,0,0,0">저장</Button>
            <TextBox Margin="5,0,0,0" VerticalAlignment="Center">스니펫 관리</TextBox>
        </DockPanel>
        <TextBox Grid.Row="1" Grid.Column="1" Margin="0,5,5,5" Text="Details View"/>

    </Grid>
</Window>
